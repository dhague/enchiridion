# CLAUDE.md

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (github.com/dhague/enchiridion), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role vocabulary, each label string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
