name: go-enchiridion

# RETIRED — placeholder only. Kept per the decision on #267 rather than
# deleted outright.
#
# This workflow previously built, vetted, and tested the Go script layer and
# released it via GoReleaser (six per-platform binaries, Chocolatey nupkg,
# Homebrew tap, checksums, build-provenance attestation). That implementation
# is gone: `enchiridion-go/` was deleted in #267, and the script layer is now
# the TypeScript bundle at `enchiridion-ts/dist/cli.js` (ADR-0017), which
# `ts-enchiridion.yml` tests on both Node and Bun and which runs through the
# `wiki-plugin/bin/enchiridion` node shim.
#
# A TS-bundle release workflow (how a distributable plugin ships the esbuild
# bundle + wasm sidecar, and what the GitHub Release attaches) is a SEPARATE
# ticket — the redesign is deferred. Until it lands, this file intentionally
# does nothing. Do not re-add Go steps: there is no `enchiridion-go/` anymore.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  placeholder:
    name: Retired — no-op
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm the Go release pipeline is retired
        run: echo "Go script layer retired with #267; a TS-bundle release workflow is a separate ticket."