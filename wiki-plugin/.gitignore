# Local dev environment + tooling caches — never committed.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/

# OpenCode wiring dev tooling (typecheck-only deps for plugins/).
wiring/opencode/node_modules/

# Resilio Sync scratch artifacts.
*.rsls

# Lazy-fetched enchiridion binaries (ADR-0013) — a cache, never committed.
.enchiridion-cache/
